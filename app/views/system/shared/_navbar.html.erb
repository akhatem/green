<nav class="navbar navbar-expand-lg navbar-light bg-light align-items-lg-start nav-bg">
    <div class="col-lg-4">
        <a href="<%= system_root_path %>" class="navbar-brand mb-3">
            <img src="<%= '/assets/main_logo.png' %>" alt="" class="logo-img">
        </a>
    </div>
    <% if current_user.roleKey.eql?("cashier") and current_user.branch_id.present? %>
        <div class="col-3 mt-2">
            <h2 class="details-tab card-title" style="color: #00AB47;">
                Branch: <%= current_user.branchName %>
            </h2>
        </div>
    <% end %>
    <div class="col-lg-5 ms-5">
        <ul class="nav justify-content-end px-5">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle user-link" style="color: #00AB47;" data-bs-toggle="dropdown" 
                    href="#" role="button" aria-expanded="false"><%= current_user.name %>
                </a>
                
                <ul class="dropdown-menu">
                    <li>
                        <%= link_to "Logout", destroy_user_session_path, method: :delete, 
                            class: "xdropdown dropdown-item" %>
                    </li>
                </ul>
                <div class="user-type"><%= current_user.role.name %></div>
            </li>
        </ul>
    </div>
</nav>